scalar DateTime

input LogInInput {
  email: String!
  password: String!
}

type Mutation {
  login(input: LogInInput!): User!
}

type Query {
  test: User
  viewer: User
}

type User {
  email: String!
  id: String!
  name: String!
}